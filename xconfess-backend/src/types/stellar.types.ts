// src/types/stellar.types.ts
// Placeholder for custom Stellar types (extend as needed)

export type StellarPublicKey = string;
export type StellarSecret = string;
export type SorobanContractId = string;
